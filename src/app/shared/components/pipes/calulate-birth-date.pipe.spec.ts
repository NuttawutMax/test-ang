import { CalulateBirthDatePipe } from './calulate-birth-date.pipe';

describe('CalulateBirthDatePipe', () => {
  it('create an instance', () => {
    const pipe = new CalulateBirthDatePipe();
    expect(pipe).toBeTruthy();
  });
});
